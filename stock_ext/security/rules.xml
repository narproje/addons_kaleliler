<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="stock_quant_access_rule_delete" model="ir.model.access">
        <field name="name">stock.quant delete access</field>
        <field name="model_id" ref="stock.model_stock_quant"/>
        <field name="group_id" ref="base.group_user"/> <!-- Burada istediğin kullanıcı grubunu belirt -->
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/> <!-- Silme izni ver -->
    </record>

</odoo>
